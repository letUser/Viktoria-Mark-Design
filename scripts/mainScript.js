"use strict"

$(showBar() {
	$("#grid2").hide(); 
	$("#start").on('click', showBar() { // указываем кнопку при клике на которую покажутся элементы
	$("#grid2").show(); // показываем элементы
		 appNormalise();
	 }) 
 });
